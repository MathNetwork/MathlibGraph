\section{Vertex Weights}

For Model~1, define the weight function $w \colon V_1 \to \mathbb{N}$:
\[
  w(m) = |\{d \in V_2 \mid d \text{ is defined in module } m\}|,
\]
i.e.\ the number of declarations contained in module~$m$.
